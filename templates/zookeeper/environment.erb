# Managed by Chef
<%-
  options={}
  options['LOG_DIR'] = node['confluent']['zookeeper']['log_dir']
  options['KAFKA_LOG4J_OPTS'] = "-Dlog4j.configuration=file:#{node['confluent']['zookeeper']['logging_config_file'] }"
  options['KAFKA_OPTS'] = node['confluent']['zookeeper']['kafka_opts']
  options['KAFKA_HEAP_OPTS'] = node['confluent']['zookeeper']['heap_opts']
  options.each do |key, value|
-%><%=key%>='<%=value%>'
<%- end -%>