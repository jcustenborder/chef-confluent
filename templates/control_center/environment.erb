# Cooked by Chef
<%-
  options={}
  options['LOG_DIR'] = node['confluent']['control_center']['log_dir']
  options['KAFKA_LOG4J_OPTS'] = "-Dlog4j.configuration=file:#{node['confluent']['control_center']['logging_config_file'] }"
  options['KAFKA_OPTS'] = node['confluent']['control_center']['kafka_opts']
  options['KAFKA_HEAP_OPTS'] = node['confluent']['control_center']['heap_opts']
  options.each do |key, value|
-%><%=key%>='<%=value%>'
<%- end -%>